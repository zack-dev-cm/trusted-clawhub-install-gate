# Corpus

This directory is reserved for audited fixture artifacts and regression cases.

`v0.1` keeps its minimal corpus in `tests/` to stay small. Expand this directory when the project starts tracking public skill samples and diffable receipts.


# Rules

This directory is reserved for versioned rule packs.

`v0.1` keeps its rules in Python source so the first release can stabilize the finding schema and CLI contract before externalizing policy data.

